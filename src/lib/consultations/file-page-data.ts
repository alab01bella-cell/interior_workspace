import { listConsultations } from "./consultation-repository";
import { listConsultationFiles } from "./consultation-file-repository";
export async function getFilePageData(workspaceId:string,requested?:string){const consultations=await listConsultations(workspaceId);const selected=consultations.find(item=>item.id===requested)??consultations[0]??null;const choices=consultations.map(item=>({id:item.id,label:`${item.clientName} · ${item.region} · ${item.area} · ${new Date(item.submittedAt).toLocaleDateString("ko-KR")}`}));const records=selected?await listConsultationFiles(workspaceId,selected.id):[];return {consultationId:selected?.id??null,choices,initialFiles:records.map(({id,fileCategory,originalFileName,fileSize,createdAt})=>({id,fileCategory,originalFileName,fileSize,createdAt}))};}
