# 기존 `Index.html` 체크리스트 전수 분석

## 0. 분석 기준

- 기준 파일: `참고/Index.html`
- 단계 수: **8개** (`section.card` 기준)
- 질문 수: **50개** (`div.field` 기준)
  - 조건부로 나타나는 `기타 공간 형태`, `기타 사유`, `원하는 스타일 기타`는 각각 독립된 `.field`이므로 질문 수에 포함했다.
  - `공간별로 바꾸고 싶은 항목` 안의 `기타 공간` 입력(`spaceDetailsOther`)은 같은 조건부 질문에 속한 하위 입력이므로 별도 질문으로 중복 계산하지 않았다.
  - 개인정보 동의도 제출을 결정하는 하나의 `.field`이므로 질문 수에 포함했다.
- 아래 문항명, `name`, 선택지의 `value`와 화면 문구는 임의로 고치지 않고 현재 HTML을 기준으로 기록했다.
- 입력 형식의 `텍스트`에는 한 줄 입력, 읽기 전용 주소 입력, 전화번호 입력 및 `textarea`를 포함하며 세부 HTML 형식은 비고에 명시했다.

## 1. 단계별 문항 명세

### 1단계. 기본 정보

단계 안내: `현장 확인과 상담 안내를 위한 기본 정보입니다.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 1 | 현장 주소 | `address` | 텍스트 | 읽기 전용 `text`. `주소 검색` 버튼으로 Daum 우편번호 검색을 열고 도로명주소 또는 지번주소를 입력한다. 공동주택이면 건물명을 괄호로 덧붙인다. |
| 2 | 상세주소 | `addressDetail` | 텍스트 | 한 줄 `text`; placeholder: `동/호수, 건물명 등 상세주소` |
| 3 | 공간 형태 | `housingType` | 단일 선택 | `아파트`, `빌라`, `단독주택`, `오피스텔`, `상가`, `기타` |
| 4 | 기타 공간 형태 | `housingTypeOther` | 텍스트 | `housingType=기타`일 때만 표시; placeholder: `공간 형태를 입력해주세요.` |
| 5 | 평수 | `areaSize` | 단일 선택 | `10평 미만~10평대`, `20평대`, `30평대`, `40평대`, `50평대`, `60평 이상` |
| 6 | 현재 상태 | `currentStatus` | 단일 선택 | `현재 공실`, `공실 예정`, `거주 중`, `입주 예정`, `신축 입주`, `기타(부동산 계약 전)` |
| 7 | 거주 형태 | `occupancyType` | 단일 선택 | `소유`, `전·월세` |
| 8 | 인테리어를 고려하게 된 이유 | `renovationReason` | 단일 선택 | `새로 입주할 공간의 리모델링`, `현재 거주 중인 공간의 리모델링`, `매매·임대를 위한 리모델링`, `기타` |
| 9 | 기타 사유 | `renovationReasonOther` | 텍스트 | `renovationReason=기타`일 때만 표시; placeholder: `인테리어를 고려하게 된 이유를 입력해주세요.` |

### 2단계. 상담 일정

단계 안내: `대면상담 및 유선안내 가능 일정을 확인하기 위한 항목입니다.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 10 | 대면상담 희망일 | `visitDate` | 날짜 | `date`; 오늘 이후의 날짜만 선택 가능하다는 안내가 있으며 실제 `min`은 오늘을 포함한다. |
| 11 | 대면상담 희망 시간 | `visitTime` | 단일 선택 | 빈 값 `시간을 선택해주세요.`, 이후 `09:00`부터 `20:00`까지 30분 간격: `09:00`, `09:30`, `10:00`, `10:30`, `11:00`, `11:30`, `12:00`, `12:30`, `13:00`, `13:30`, `14:00`, `14:30`, `15:00`, `15:30`, `16:00`, `16:30`, `17:00`, `17:30`, `18:00`, `18:30`, `19:00`, `19:30`, `20:00` |
| 12 | 유선안내 가능 요일 | `callDays` | 복수 선택 | `월요일`, `화요일`, `수요일`, `목요일`, `금요일`, `토요일`, `상관없음` |
| 13 | 유선안내 가능 시간 | `callTime` | 단일 선택 | 빈 값 `시간을 선택해주세요.`, 이후 `09:00`부터 `20:00`까지 30분 간격: `09:00`, `09:30`, `10:00`, `10:30`, `11:00`, `11:30`, `12:00`, `12:30`, `13:00`, `13:30`, `14:00`, `14:30`, `15:00`, `15:30`, `16:00`, `16:30`, `17:00`, `17:30`, `18:00`, `18:30`, `19:00`, `19:30`, `20:00` |

### 3단계. 공사 범위 및 우선순위

단계 안내: `원하는 공사 범위와 가장 중요하게 생각하는 부분을 알려주세요.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 14 | 공사 범위 | `constructionScope` | 단일 선택 | `전체공사`, `부분공사`, `상담 필요` |
| 15 | 바꾸고 싶은 공간 | `targetSpaces` | 복수 선택 | `전체`, `현관`, `거실`, `주방`, `욕실`, `방`, `베란다`, `다용도실`, `기타`. 단, `전체`와 개별 공간은 동시에 유지되지 않도록 동작한다. |
| 16 | 공간별로 바꾸고 싶은 항목 | `spaceDetails`; 하위 기타 입력 `spaceDetailsOther` | 복수 선택; 조건부 텍스트 | `targetSpaces` 선택에 따라 아래의 공간별 항목이 조건부 표시된다. |
| 17 | 현재 공간에서 가장 불편하거나 개선하고 싶은 부분 | `inconvenience` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 수납이 부족해요 / 주방이 좁고 불편해요 / 공간이 전체적으로 어두워 보여요` |
| 18 | 하지 않아도 되는 공사 | `skipOk` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 베란다는 꼭 안 해도 됩니다. 창호는 상태를 보고 결정하고 싶어요.` |
| 19 | 가장 중요하게 생각하는 기준 | `priority` | 복수 선택 | `예산`, `디자인`, `수납`, `동선`, `마감`, `일정`, `내구성` |
| 20 | 절대 포기하기 어려운 부분 | `nonNegotiable` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 욕실은 꼭 새것처럼 바꾸고 싶어요. 수납은 최대한 확보하고 싶어요.` |

#### 공간 선택에 따른 조건부 항목

`targetSpaces`에서 선택한 공간의 그룹만 표시한다. `전체`를 선택하면 모든 그룹을 표시한다. 그룹이 숨겨질 때 해당 그룹의 체크와 텍스트 값은 즉시 초기화된다.

| `targetSpaces` 선택 | 표시되는 그룹 | field name | 입력 형식 | 모든 선택지 / 입력 |
|---|---|---|---|---|
| `현관` | 현관 | `spaceDetails` | 복수 선택 | 표시 문구 `중문`, `신발장`, `바닥`, `조명`; 실제 값 `현관 · 중문`, `현관 · 신발장`, `현관 · 바닥`, `현관 · 조명` |
| `거실` | 거실 | `spaceDetails` | 복수 선택 | 표시 문구 `바닥`, `벽·천장`, `조명`, `수납`; 실제 값 `거실 · 바닥`, `거실 · 벽·천장`, `거실 · 조명`, `거실 · 수납` |
| `주방` | 주방 | `spaceDetails` | 복수 선택 | 표시 문구 `싱크대·가구`, `상판`, `벽타일`, `수납·동선`; 실제 값 `주방 · 싱크대·가구`, `주방 · 상판`, `주방 · 벽타일`, `주방 · 수납·동선` |
| `욕실` | 욕실 | `spaceDetails` | 복수 선택 | 표시 문구 `타일`, `세면대·양변기`, `수전·샤워`, `욕조·파티션`; 실제 값 `욕실 · 타일`, `욕실 · 세면대·양변기`, `욕실 · 수전·샤워`, `욕실 · 욕조·파티션` |
| `방` | 방 | `spaceDetails` | 복수 선택 | 표시 문구 `바닥`, `벽·천장`, `붙박이장`, `조명`; 실제 값 `방 · 바닥`, `방 · 벽·천장`, `방 · 붙박이장`, `방 · 조명` |
| `베란다` | 베란다 | `spaceDetails` | 복수 선택 | 표시 문구 `바닥`, `탄성코트·도장`, `창호`, `수납`; 실제 값 `베란다 · 바닥`, `베란다 · 탄성코트·도장`, `베란다 · 창호`, `베란다 · 수납` |
| `다용도실` | 다용도실 | `spaceDetails` | 복수 선택 | 표시 문구 `바닥`, `수납`, `세탁기·건조기 공간`, `배수·설비`; 실제 값 `다용도실 · 바닥`, `다용도실 · 수납`, `다용도실 · 세탁기·건조기 공간`, `다용도실 · 배수·설비` |
| `기타` | 기타 공간 | `spaceDetailsOther` | 텍스트 | 여러 줄 `textarea`; placeholder: `공간과 바꾸고 싶은 항목을 입력해주세요.` |
| `전체` | 위의 8개 그룹 전체 | 위와 같음 | 위와 같음 | 모든 공간별 선택지 및 기타 공간 입력을 함께 표시 |

### 4단계. 예산 및 일정

단계 안내: `예산은 정확하지 않아도 됩니다. 상담 기준을 잡기 위한 항목입니다.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 21 | 생각 중인 예산 | `budget` | 텍스트 | `inputmode=numeric`, 화면 접미사 `만원`. 숫자가 아닌 문자를 제거하고 천 단위 쉼표를 자동 삽입한다. |
| 22 | 예산 기준 | `budgetType` | 단일 선택 | `최대한 절약`, `퀄리티 우선`, `상담 필요` |
| 23 | 입주 예정일 | `moveInDate` | 날짜 | `date`; 오늘 이후의 날짜만 선택 가능하다는 안내가 있으며 실제 `min`은 오늘을 포함한다. |
| 24 | 공사 희망 시기 | `preferredStart` | 단일 선택 | `최대한 빠르게`, `1개월 이내`, `2~3개월 이내`, `협의 후 진행` |
| 25 | 공사 중 거주 여부 | `livingDuringConstruction` | 단일 선택 | `공실 상태에서 공사`, `거주하면서 공사` |
| 26 | 일정 관련 특이사항 | `scheduleNote` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 이사일이 정해져 있어요. 특정 날짜 전까지 끝나야 해요.` |

### 5단계. 원하는 분위기

단계 안내: `정확한 스타일명을 몰라도 괜찮습니다. 느낌만 체크해주세요.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 27 | 원하는 스타일 | `styles` | 복수 선택 | 표시 문구 기준 `화이트`, `우드`, `모던`, `미니멀`, `내추럴`, `따뜻한 느낌`, `고급스러운 느낌`, `기타`. 단, `미니멀`의 실제 `value`는 `심플`이다. |
| 28 | 원하는 스타일 기타 | `otherStyle` | 텍스트 | `styles`의 `기타`가 체크될 때만 표시되는 여러 줄 `textarea`; placeholder: `원하는 스타일이나 분위기를 자유롭게 적어주세요.` |
| 29 | 선호 색감 | `colorTone` | 복수 선택 | `밝은 톤`, `어두운 톤`, `베이지톤`, `그레이톤`, `우드톤`, `컬러 포인트`, `상담 필요` |
| 30 | 피하고 싶은 느낌 | `avoidStyle` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 너무 차가운 느낌은 싫어요. 어두운 집은 피하고 싶어요.` |

### 6단계. 생활 방식

단계 안내: `생활 패턴에 따라 수납, 주방, 마감 방향이 달라질 수 있습니다.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 31 | 거주 인원 | `residents` | 단일 선택 | `1인`, `2인`, `3인`, `4인 이상` |
| 32 | 아이 여부 | `hasChild` | 단일 선택 | `있음`, `없음` |
| 33 | 반려동물 여부 | `hasPet` | 단일 선택 | `있음`, `없음` |
| 34 | 수납 필요도 | `storageNeed` | 단일 선택 | `적음`, `보통`, `많음` |
| 35 | 요리 빈도 | `cookingFrequency` | 단일 선택 | `거의 안 함`, `보통`, `자주 함` |
| 36 | 재택근무·작업공간 | `workSpace` | 단일 선택 | `필요`, `필요 없음`, `상담 필요` |
| 37 | 생활 방식 및 취미 관련 특이사항 | `lifestyleNote` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 홈짐이 필요해요 / 악기 연주를 해요 / 캠핑 장비가 많아요 / 큰 작품을 만드는 취미가 있어요` |

### 7단계. 사진 및 참고자료

단계 안내: `현재 집 사진이나 원하는 분위기 자료가 있으면 첨부해주세요.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 38 | 현장 사진 | `sitePhotos` | 파일 첨부 | `accept=image/*`, `multiple`; 안내: `현재 집 사진을 첨부해주세요. 여러 장 선택 가능합니다.` |
| 39 | 참고 이미지 | `referenceImages` | 파일 첨부 | `accept=image/*`, `multiple`; 안내: `원하는 분위기, 타업체 시공사례, 캡처 이미지 등을 첨부할 수 있습니다.` |
| 40 | 참고 링크 | `referenceLinks` | 텍스트 | 여러 줄 `textarea`; placeholder: `인스타그램 / 블로그 / 핀터레스트 / 타업체 시공사례 / 제품 링크 등을 붙여넣어주세요.` |
| 41 | 참고 자료에서 마음에 드는 부분 | `referenceLike` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 전체 분위기 / 색감 / 주방 디자인 / 욕실 분위기 / 조명 / 수납 방식 / 바닥재 느낌 / 가구 배치` |

### 8단계. 상담 및 연락 정보

단계 안내: `상담 방식과 연락 정보를 마지막으로 확인해주세요.`

| # | 질문 | field name | 입력 형식 | 모든 선택지 / 현재 동작 |
|---:|---|---|---|---|
| 42 | 연령대 | `ageGroup` | 단일 선택 | `20~30대`, `40대`, `50대`, `60대 이상` |
| 43 | 인테리어 상담 경험 | `consultationExperience` | 단일 선택 | `없음`, `1~2회`, `3회 이상` |
| 44 | 상담 시 원하는 의견 반영 방식 | `decisionStyle` | 단일 선택 | `제 의견을 중심으로 진행하고 싶어요`, `전문가의 추천을 참고해서 결정하고 싶어요`, `전문가에게 전체적으로 맡기고 싶어요` |
| 45 | 상담 안내를 받으실 연락 방법 | `preferredContact` | 단일 선택 | `전화`, `문자·카카오톡` |
| 46 | 상담 때 꼭 물어보고 싶은 내용 | `questions` | 텍스트 | 여러 줄 `textarea`; placeholder: `예: 이 예산으로 어디까지 가능한지 궁금해요. 공사 기간이 얼마나 걸릴지 궁금해요.` |
| 47 | 기타 요청사항 | `etc` | 텍스트 | 여러 줄 `textarea`; placeholder: `추가로 전달하고 싶은 내용을 적어주세요.` |
| 48 | 성함 | `name` | 텍스트 | 한 줄 `text`; placeholder: `성함을 입력해주세요.` |
| 49 | 휴대폰 번호 | `phone` | 텍스트 | `tel`, `inputmode=numeric`; 입력 중 자동 하이픈 처리 |
| 50 | 개인정보 수집 및 이용에 동의합니다. | `privacyConsent` | 단일 선택 | 체크박스 값 `동의`. 안내: `상담 접수와 연락을 위해 성명, 연락처, 주소 및 상담 내용을 수집하며 상담 완료 후 관련 기준에 따라 보관·파기합니다.` |

## 2. 기타 선택 시 나타나는 입력란

| 원본 질문과 조건 | 나타나는 질문 | field name | 숨김/초기화 동작 |
|---|---|---|---|
| `housingType`에서 `기타` 선택 | 기타 공간 형태 | `housingTypeOther` | 다른 라디오를 고르면 숨기고 값을 비운다. |
| `renovationReason`에서 `기타` 선택 | 기타 사유 | `renovationReasonOther` | 다른 라디오를 고르면 숨기고 값을 비운다. |
| `styles`에서 `기타` 체크 | 원하는 스타일 기타 | `otherStyle` | 체크를 해제하면 숨기지만, 현재 구현은 값을 비우지 않는다. 따라서 숨은 값이 제출될 수 있다. |
| `targetSpaces`에서 `기타` 체크 | 공간별 항목의 기타 공간 입력 | `spaceDetailsOther` | `기타`를 해제하거나 다른 조건으로 그룹이 숨으면 값을 비운다. |

`currentStatus`의 `기타(부동산 계약 전)`은 이름에 `기타`가 들어가지만 별도 입력란을 표시하지 않는다.

## 3. 입력 보정 및 검증 동작

### 휴대폰 번호 자동 하이픈

- `phone` 입력에서 숫자가 아닌 문자를 제거하고 최대 11자리까지만 남긴다.
- 3자리 이하는 그대로 표시한다.
- 4~7자리는 `3자리-나머지` 형식이다.
- 8~11자리는 `3자리-4자리-나머지` 형식이다.
- 제출 데이터 수집 시 같은 formatter를 한 번 더 적용한다.
- 주의: 10자리 번호도 클라이언트에서는 `3-4-3`으로 포맷된다. 반면 `code.gs`의 의도된 서버 formatter는 10자리를 `3-3-4`로 다루므로 규칙이 충돌한다.

### 예산 숫자 제한 및 천 단위 쉼표

- `budget`은 HTML `type=number`가 아니라 `type=text`, `inputmode=numeric`이다.
- `input` 이벤트마다 모든 비숫자를 제거하고 `/\B(?=(\d{3})+(?!\d))/g`로 천 단위 쉼표를 넣는다.
- 키 입력 단계에서는 숫자, Backspace/Delete/방향키/Home/End/Tab, 복사·붙여넣기 관련 단축키만 허용한다. 붙여넣은 값도 `input` 처리에서 숫자만 남는다.
- **숫자 이외 입력 제한은 있으나 최대 자릿수, 최소값, 최대값 또는 금액 상한은 없다.**
- 화면 단위는 `만원`이다. 기존 `code.gs`에는 숫자를 다시 뽑은 뒤 `천만원`을 붙이는 `formatBudgetCheonman_`이 있어 단위가 충돌한다.

### 과거 날짜 선택 제한

- `visitDate`, `moveInDate` 모두 사용자의 로컬 날짜 기준 오늘을 계산해 `min`으로 설정한다.
- 기존 값이 오늘보다 과거이면 초기화한다.
- `change` 시 과거 날짜면 초기화하고 `지난 날짜는 선택할 수 없습니다.`를 표시한다.
- 화면 안내는 `오늘 이후의 날짜만 선택할 수 있습니다.`이지만 구현은 오늘도 선택할 수 있다.

### 개인정보 동의 필수 처리

- 제출 이벤트에서 `privacyConsent.checked`를 검사하며, 미동의 시 제출을 중단하고 `제출하려면 개인정보 수집 및 이용에 동의해주세요.`를 표시한 뒤 체크박스로 포커스를 이동한다.
- HTML의 체크박스 자체에는 `required` 속성이 없고 `form`에는 `novalidate`가 있다. 따라서 필수 처리는 현재 JavaScript 제출 핸들러에만 의존한다.
- 개인정보 동의를 제외한 모든 문항에는 `required` 검증이 없다. `다음` 이동도 현재 단계의 입력 여부를 검증하지 않는다.

## 4. 사진 및 참고 이미지 첨부 방식

- `sitePhotos`와 `referenceImages`는 각각 `accept="image/*"`, `multiple` 파일 입력이다.
- 브라우저에서 선택 개수 자체를 제한하지는 않지만, 제출 시 각 입력별 `files.slice(0, 10)`만 처리하므로 **각 종류별 최대 10개만 실제 payload에 포함**된다. 10개 초과에 대한 화면 경고는 없다.
- 각 파일은 `FileReader.readAsDataURL`로 읽고 data URL의 쉼표 뒤 Base64만 추출해 `{ name, mimeType, base64 }` 형태로 만든다.
- payload는 `{ formData, files: { sitePhotos, referenceImages } }` 구조다.
- 파일 크기 제한, 전체 payload 크기 제한, 확장자/실제 MIME 재검증, 미리보기, 개별 삭제 UI, 업로드 진행률은 없다.
- 로딩 화면에는 `사진 용량이 크면 시간이 조금 걸릴 수 있습니다.`만 표시된다.
- 기존 Apps Script의 의도는 Base64를 Blob으로 바꿔 Google Drive 폴더에 저장하고 URL을 시트에 기록하는 방식이다. React 이전에서는 별도의 파일 저장 API/스토리지 및 메타데이터 보관 방식이 필요하다.

## 5. 다음·이전 이동과 진행률 표시

- `cards` 배열 순서가 단계 순서이며 `currentStep`은 0에서 시작한다.
- 이전 버튼: 첫 단계에서는 숨김. 클릭 시 `currentStep--`, 단계 갱신 후 페이지 최상단으로 부드럽게 이동한다.
- 다음 버튼: 마지막 단계에서는 숨김. 클릭 시 `currentStep++`, 단계 갱신 후 페이지 최상단으로 부드럽게 이동한다.
- 제출 버튼: 마지막 단계에서만 표시한다.
- 진행 문구: `${현재 단계(1 기준)} / ${전체 단계}`와 현재 카드의 `data-title`을 표시한다.
- 진행 바: `((currentStep + 1) / total) * 100`이므로 첫 화면부터 12.5%, 마지막은 100%다.
- 이동 시 오류 메시지를 숨긴다.
- 단계별 필수값 검증, 미작성 표시, URL/브라우저 history 연동, 새로고침 후 단계 복원은 없다.

## 6. 상담목록 요약 데이터와 기존 필드 매핑

현재 React 상담목록의 `Consultation` 타입, 데스크톱/모바일 목록, 원본 보기 모달을 기준으로 한 매핑이다. `Index.html`에 직접 존재하지 않는 값은 파생 또는 별도 시스템 값으로 구분한다.

### 목록 및 타입 전체 매핑

| 상담목록 데이터 | 목록/모달 용도 | 기존 체크리스트 매핑 | 변환 또는 결정 필요 사항 |
|---|---|---|---|
| `id` | 원본 식별자 | 직접 필드 없음 | 서버/DB에서 접수 ID 생성 필요 |
| `status` | 상태 표시·필터·관리 (`접수`, `예약`, `완료`, `계약`) | 직접 필드 없음 | 신규 접수 기본값 및 상태 변경 저장 정책 필요 |
| `customerName` | 고객 이름, 검색 | `name` | 키 이름 변환 필요 |
| `phone` | 원본 모달 연락처 | `phone` | 자동 하이픈 결과를 저장할지 숫자 정규형을 저장할지 결정 필요 |
| `region` | 목록 지역, 검색 | `address`에서 파생 | 현재 정확한 분리 규칙 없음. 주소 API의 시/도·시/군/구 구조 데이터를 별도로 보존하는 편이 필요 |
| `fullAddress` | 원본 모달 현장 주소 | `address` + `addressDetail` | 공백으로 결합. 기존 Apps Script도 이 방식 |
| `housingType` | 원본 모달 공간 정보 | `housingType`; 필요 시 `housingTypeOther` | `기타`일 때 표시값을 `기타`로 둘지 직접 입력값으로 치환할지 결정 필요 |
| `areaSize` | 목록 평수, 모달 공간 정보 | `areaSize` | 체크리스트는 평수 구간, 현재 mock은 `24평` 등 정확한 평수라 데이터 의미가 다름 |
| `visitDate` | 목록/모달 상담 희망일 | `visitDate` | 직접 매핑 |
| `visitTime` | 목록/모달 상담 희망 시간 | `visitTime` | 직접 매핑 |
| `budget` | 목록/모달 예상 금액 | `budget` | 체크리스트 값은 쉼표가 포함된 문자열이므로 쉼표 제거 후 숫자 변환 필요. 단위는 `만원`으로 통일 필요 |
| `receivedAt` | 접수일, 최신순 정렬 | 직접 필드 없음 | 서버 수신 시각 생성 필요. 클라이언트 시각을 신뢰하지 않는 것이 필요 |
| `style` | 모달 `원하는 분위기` | `styles` + `otherStyle` + `colorTone` 후보 | 현재 단일 문자열 조합 규칙과 `기타` 처리 규칙 없음 |
| `family` | 모달 `거주 구성` | `residents` + `hasChild` (+ 필요 시 `hasPet`) 후보 | 현재 `부부와 자녀 1명` 같은 mock 문구를 만들 정보가 부족하며 조합 규칙 없음 |
| `request` | 모달 `주요 요청사항` | `inconvenience`, `skipOk`, `nonNegotiable`, `questions`, `etc`, `lifestyleNote` 중 하나 또는 조합 후보 | 어느 문항을 대표 요약으로 쓸지 규칙 없음 |

### 화면별 표시 항목

- 데스크톱 목록: `status`, `customerName`, `region`, `areaSize`, `visitDate`, `visitTime`, `budget`, `receivedAt`, 원본 보기, 상태 관리.
- 모바일 목록: `status`, `customerName`, `receivedAt`, `region`, `areaSize`, `visitDate`, `visitTime`, `budget`, 원본 보기, 상태 관리.
- 원본 보기 모달: `id`, `customerName`, `status`, `receivedAt`, `phone`, `fullAddress`, `housingType`, `areaSize`, `visitDate`, `visitTime`, `budget`, `style`, `family`, `request`.
- 현재 상담목록은 mock 데이터만 사용하며 체크리스트 응답 전체 50개를 표현하는 타입/원본 상세 구조는 아직 없다.

## 7. React 이전 시 제거해야 할 기능

아래는 체크리스트 문항 기능이 아니라 정적 HTML 자체를 현장에서 편집하기 위한 기능이므로 React 이전 대상에서 제거해야 한다.

1. **편집 모드**
   - 상단 `editor-toolbar`, `edit-help`, `편집 모드/편집 종료`, `저장`, 편집 상태 문구.
   - `prepareLiveEditor`, `markEditable`, `assignEditorIds`, `setEditMode`, `synchronizeEditableValues`, `flashEditorStatus` 및 관련 CSS/data attribute/contenteditable 처리.
2. **HTML 다운로드**
   - `editorDownload` 버튼과 `downloadEditedHtml` 전체.
   - DOM clone, Blob 생성, `interior-consultation-edited.html` 다운로드 처리.
3. **localStorage 문구 편집**
   - 키 `interior-checklist-live-copy-v10`.
   - `getEditorCopy`, `saveEditorCopy`, `loadEditorCopy` 및 편집 문구/placeholder 저장·복원.
4. **`google.script.run`**
   - 로드 시 `setupSheet()` 호출.
   - 제출 시 실행 환경 확인과 `submitChecklist(payload)` 호출, Apps Script 성공/실패 handler.
   - React에서는 실제 백엔드 API 계약으로 교체해야 하며, 기존 호출을 그대로 보존하면 안 된다.

문항 값 자체는 편집 모드의 `synchronizeEditableValues()`에 의존하지 않도록 고정된 schema로 정의해야 한다. 특히 현재 편집 모드는 화면 선택지 문구를 수정하면 input `value`도 바꾸므로 저장 데이터 규격이 사용자 로컬 편집에 따라 달라질 수 있다.

## 8. React 이전 시 누락·충돌 가능성 및 발견 문제

### A. 현재 `Index.html` 내부의 불일치/주의사항

1. `styles`의 화면 문구 `미니멀`과 실제 저장 값 `심플`이 다르다. React schema에서 label/value를 그대로 구분하지 않으면 기존 데이터와 충돌한다.
2. `otherStyle`은 `기타` 체크 해제 시 숨기기만 하고 값을 비우지 않아 숨은 값이 제출될 수 있다. 다른 기타 입력 두 개는 숨길 때 값을 비우므로 동작도 일관되지 않다.
3. 날짜 안내는 “오늘 이후”지만 `min=오늘`이라 오늘도 허용한다.
4. 예산의 화면 단위는 `만원`인데 기존 Apps Script formatter는 `천만원`을 붙이려 한다.
5. 휴대폰 클라이언트 formatter는 10자리 번호를 `3-4-3`으로 만들지만 서버 formatter의 의도는 `3-3-4`다.
6. `callDays`의 `상관없음`은 다른 요일들과 동시에 선택할 수 있다. `targetSpaces`의 `전체`만 상호 배타 처리가 있다.
7. 파일 입력은 UI상 무제한 다중 선택처럼 보이나 실제 전송은 종류별 앞 10개뿐이며 초과 파일 누락을 알리지 않는다.
8. 모든 일반 질문이 선택 사항이다. 이름, 휴대폰, 주소조차 비어 있는 채 개인정보 동의만 하면 제출 이벤트를 통과한다.
9. 개인정보 체크박스에 native `required`가 없고 `novalidate`라 JavaScript가 실행되지 않거나 제출 경로가 달라지면 필수 보장이 없다.
10. `accept=image/*`는 브라우저 선택 힌트일 뿐 보안 검증이 아니다. 크기 제한도 없다.
11. 편집 모드에서 선택지 문구를 바꾸면 `value`도 바뀌어 데이터 규격이 고정되지 않는다.
12. Daum 주소 스크립트는 외부 전역 객체와 inline `onclick`에 의존한다. React 이전 시 로드 실패, CSP, SSR 및 이벤트 연결을 별도로 처리해야 한다.
13. 진행률은 문항 작성률이 아니라 단계 위치이며 첫 단계부터 12.5%다.
14. 이전/다음 이동에 단계별 검증이 없고 새로고침 시 현재 단계와 입력 상태가 복원되지 않는다.

### B. 현재 HTML과 `collectFormData()`의 충돌

`collectFormData()`가 명시적으로 빈 배열을 만드는 체크박스 이름에는 현재 HTML에 없는 구버전 필드가 섞여 있다.

- 현재 존재: `callDays`, `targetSpaces`, `spaceDetails`, `styles`, `colorTone`, `priority`
- 현재 HTML에 없음: `consultPurpose`, `replaceItems`, `mustSpaces`

따라서 payload에는 화면에 없는 `consultPurpose`, `replaceItems`, `mustSpaces`가 빈 배열로 생성된다. 반대로 개인정보 동의는 일반 `FormData` 흐름으로 들어가며, 파일은 별도 `files`로 빠진다.

### C. 현재 HTML과 기존 `참고/code.gs` 저장 스키마의 충돌

기존 Apps Script는 현재 문항보다 오래된 schema를 기준으로 하며 다음 문제가 있다.

- 현재 HTML에 있으나 시트 row에 저장되지 않는 필드: `addressDetail`은 `address`와 결합되어 저장되므로 예외적으로 보존되지만, `housingTypeOther`, `occupancyType`, `renovationReason`, `renovationReasonOther`, `spaceDetails`, `spaceDetailsOther`, `lifestyleNote`, `ageGroup`, `consultationExperience`, `decisionStyle`, `preferredContact`, `privacyConsent`는 독립 저장 대상이 없다.
- Apps Script가 기대하지만 현재 HTML에 없는 필드: `consultPurpose`, `replaceItems`, `keepItems`, `mustDo`, `focusSpaces`, `mustSpaces`, `reduceItems`, `referenceDislike`.
- Apps Script 헤더의 `주거 형태`는 실제로 `housingType`을 저장한다. 현재 HTML에는 별도로 `거주 형태(occupancyType)`가 있으므로 용어와 데이터가 충돌한다.
- 현재 질문은 `가장 중요하게 생각하는 기준`인데 시트 헤더는 `가장 중요하게 생각하는 부분`이다.
- 파일 URL 결합 코드가 현재 보이는 소스 기준 `join('n')`이어서 실제 줄바꿈 `\n` 의도와 다를 가능성이 있다.
- 제공된 `code.gs`에는 `||`, `&&`, `=>`, 정규식 구분자, 객체 콜론 등이 빠져 보이는 다수의 문법 이상이 있다. 이 파일이 실제 배포 원본과 동일한지 먼저 확인해야 하며, 동일하다면 실행 불가능한 수준이다.

### D. 상담목록과의 schema 충돌

1. 목록 타입은 15개 요약 필드만 가지며 체크리스트 전체 응답을 보관하거나 원본 그대로 표시할 구조가 없다.
2. `customerName` 대 `name`, `fullAddress` 대 `address/addressDetail`처럼 이름 변환이 필요하다.
3. `region`, `id`, `status`, `receivedAt`은 체크리스트 입력 필드가 아니므로 서버 파생/관리 데이터가 필요하다.
4. `style`, `family`, `request`는 현재 여러 원본 필드에서 합성해야 하나 조합 규칙이 없다.
5. `areaSize`는 체크리스트의 구간 값과 상담목록 mock의 정확한 평수 값이 다르다.
6. `budget`은 체크리스트에서 쉼표 포함 문자열, 상담목록에서 `number`다.
7. 상담목록 검색은 고객 이름과 `region`만 대상으로 하므로 주소 파생 실패 시 지역 검색이 동작하지 않는다.
8. 모달 명칭은 `상담 원본`이지만 현재 타입은 전체 원본 문항을 담지 않고 세 개의 합성 요약(`style`, `family`, `request`)만 보여준다.

### E. React 이전 시 반드시 보존하거나 결정할 동작

- 8단계 순서, 단계 제목, 모든 질문/선택지의 label과 value를 별도 관리할지 여부.
- 조건부 기타 필드의 표시와 숨길 때 값 초기화 정책.
- `targetSpaces=전체` 상호 배타 처리와 공간별 세부 그룹 표시/숨김 및 숨김 시 초기화.
- 전화번호와 예산의 저장 정규형 및 화면 표시형 분리.
- 날짜의 “오늘 포함” 여부를 문구 또는 로직 중 어느 쪽에 맞출지 결정.
- 개인정보 동의의 UI와 클라이언트/서버 양쪽 필수 검증.
- 이미지별 10개 제한을 유지할지, 화면에서 명시할지, 파일 크기/MIME 제한을 추가할지 결정.
- 체크리스트 원본 전체 schema와 상담목록용 파생 요약 schema를 분리하고 매핑 규칙을 고정.
- 주소 검색에서 표시 문자열 외에 지역 파생에 필요한 구조화 주소 데이터를 보존할지 결정.

